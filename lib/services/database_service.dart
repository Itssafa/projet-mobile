// Ce fichier n'est plus utilisé - l'application utilise maintenant un stockage en mémoire
// Conservé pour référence historique uniquement
// Toutes les fonctionnalités de base de données ont été migrées vers AnimalProvider

// Classe désactivée - utilisation du stockage en mémoire via AnimalProvider
class DatabaseService {
  // Cette classe n'est plus utilisée
  // L'application utilise maintenant AnimalProvider avec stockage en mémoire
  // Tous les CRUD sont gérés par AnimalProvider dans lib/providers/animal_provider.dart
}
